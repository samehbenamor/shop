import { InventoryItemDTO, MedusaContainer, ProductTypes } from "@medusajs/types";
import { EntityManager } from "typeorm";
export declare function detachInventoryItems({ container, manager, data, }: {
    container: MedusaContainer;
    manager: EntityManager;
    data: {
        variant: ProductTypes.ProductVariantDTO;
        inventoryItem: InventoryItemDTO;
    }[];
}): Promise<void[]>;
