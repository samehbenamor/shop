import { MedusaContainer, ProductTypes } from "@medusajs/types";
import { EntityManager } from "typeorm";
export declare function createInventoryItems({ container, manager, data, }: {
    container: MedusaContainer;
    manager: EntityManager;
    data: ProductTypes.ProductDTO[];
}): Promise<({
    variant: ProductTypes.ProductVariantDTO;
    inventoryItem: import("@medusajs/types").InventoryItemDTO;
} | undefined)[]>;
