export declare type CreateProductCollection = {
    title: string;
    handle?: string;
    metadata?: Record<string, unknown>;
};
export declare type UpdateProductCollection = {
    title?: string;
    handle?: string;
    metadata?: Record<string, unknown>;
};
