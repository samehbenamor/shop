import { FindConfig, DAL, Context } from "@medusajs/types";
declare type RetrieveEntityParams<TDTO> = {
    id: string;
    entityName: string;
    repository: DAL.TreeRepositoryService | DAL.RepositoryService;
    config: FindConfig<TDTO>;
    sharedContext?: Context;
};
export declare function retrieveEntity<TEntity, TDTO>({ id, entityName, repository, config, sharedContext, }: RetrieveEntityParams<TDTO>): Promise<TEntity>;
export {};
